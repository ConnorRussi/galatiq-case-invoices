# Configuration

`config.toml` sets the TAMU model ID and request timeout plus file-size, PDF-page, text-character, and minimum-PDF-text limits. The provider reads `TAMU_CHAT_API_KEY` and `TAMU_CHAT_BASE_URL` from `.env`.
